<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.ArticleMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.entity.Article">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="content" property="content" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, title, content
    </sql>

    <!-- 逻辑删除文章 -->
    <update id="logicDeleteById">
        UPDATE article
        SET is_deleted = 1,
            update_time = NOW()
        WHERE id = #{id}
          AND is_deleted = 0
    </update>


    <!-- 博客首页分页查询文章（包含附件信息） -->
    <select id="selectArticlePageWithAttachments" resultMap="BaseResultMap">
        SELECT DISTINCT
            a.id, a.title, a.content, a.author_id, a.author_name,
            a.create_time, a.update_time, a.is_deleted, a.status,
            a.summary, a.cover_image, a.category_id, a.tags,
            a.view_count, a.like_count, a.comment_count, a.sort
        FROM article a
                 LEFT JOIN blog_attachments ba ON a.id = ba.article_id AND ba.is_delete = 0
        WHERE a.is_deleted = 0 and a.status = 1
        ORDER BY a.create_time DESC
    </select>


    <!-- 统计已发布文章数量 -->
    <select id="selectPublishedCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM article
        WHERE is_deleted = 0 AND status = 1
    </select>

    <!-- 统计草稿文章数量 -->
    <select id="selectDraftCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM article
        WHERE is_deleted = 0 AND status = 0
    </select>

    <!-- 统计总浏览量 -->
    <select id="selectTotalViewCount" resultType="java.lang.Long">
        SELECT COALESCE(SUM(view_count), 0)
        FROM article
        WHERE is_deleted = 0
    </select>

    <!-- 按日期范围查询文章列表 -->
    <select id="selectArticleListByDateRange" resultType="java.util.HashMap">
        SELECT
        DATE(create_time) as article_date,
        COUNT(*) as article_count,
        SUM(view_count) as total_views,
        SUM(like_count) as total_likes
        FROM article
        WHERE is_deleted = 0
        <if test="startDate != null and startDate != ''">
            AND create_time &gt;= #{startDate}
        </if>
        <if test="endDate != null and endDate != ''">
            AND create_time &lt;= #{endDate}
        </if>
        GROUP BY DATE(create_time)
        ORDER BY article_date DESC
    </select>




    <!-- 分类统计 -->
    <select id="selectCategoryStatistics" resultType="java.util.HashMap">
        SELECT
            category_id,
            COUNT(*) as article_count,
            SUM(view_count) as total_views,
            SUM(like_count) as total_likes
        FROM article
        WHERE is_deleted = 0 AND category_id IS NOT NULL
        GROUP BY category_id
        ORDER BY article_count DESC
    </select>

    <!-- 标签统计 -->
    <select id="selectTagStatistics" resultType="java.util.HashMap">
        SELECT
            tags,
            COUNT(*) as article_count,
            SUM(view_count) as total_views
        FROM article
        WHERE is_deleted = 0 AND tags IS NOT NULL AND tags != ''
        GROUP BY tags
        ORDER BY article_count DESC
            LIMIT 20
    </select>
</mapper>
